# Current Context

**Phase:** 5 / 10 — Asset Calibration (BLOCKED) + Phase 10 Deploy
**Current task:** 5.1 — waiting for real iPhone PNG mockup files
**Status:** blocked on PNGs; everything else is complete and building

## What's done
- All phases 1–4, 6–9 complete and committed
- Production build passes: `npm run build` ✓
- Dev server runs at http://localhost:5173
- App renders: empty state → upload screenshot → see it in placeholder frame
- Export works (1x/2x/3x PNG download)
- Dark mode, keyboard shortcuts (U/D/R), all controls wired

## What's blocked
- Phase 5: real iPhone PNG mockups needed in `public/mockups/`
  See BLOCKERS.md for filenames and download sources.

## What's next (concrete)
1. User provides PNG mockups → place in `public/mockups/`
2. Open each PNG in image editor, read real screenX/Y/Width/Height/CornerRadius
3. Update `src/lib/frames.ts`, remove all `// TODO: calibrate` comments
4. Verify alignment in running app
5. Then: write README.md, deploy to Vercel (task 10.1–10.5)

## Key files
- `src/lib/frames.ts` — device metadata (needs calibrated coords)
- `src/lib/canvas.ts` — rendering engine
- `src/components/MockupCanvas.tsx` — wires canvas to store
- `src/App.tsx` — main layout
